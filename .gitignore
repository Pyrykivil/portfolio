# Portable ffmpeg (Windows binary, regenerated by scripts/ingest_media.py)
tools/

# Runtime + local artifacts
content.backups/
messages/
NEXT_SESSION.md

# CV pdf lives on the server, uploaded via /admin/ (POST /api/cv)
assets/cv/

# Secrets (only .env.example is tracked)
.env
deploy/.env

# Python
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db
