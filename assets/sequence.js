/* Media manifest. Updated automatically after frame extraction.
   frames: 0 keeps the site in fallback mode (animated teal void). */
window.PORTFOLIO_MEDIA = {
  hero: { frames: 241, base: "assets/frames/hero/f_", pad: 4, ext: ".jpg" }
};
